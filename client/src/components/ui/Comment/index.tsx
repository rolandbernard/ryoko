import './comment.scss';

interface Props {
    comment: string;
}

export default function Comment({ comment }: Props) {
    return (
        <div className="comment-container">
            
        </div>
    )
}